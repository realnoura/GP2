import 'package:hadq/classes/child.dart';

class Progress {
  //Child child = child;

  void sendWeeklyProgress(child) {
    //checking if 7 days and send email
  }
  int get score {
    return 1;
    // return child.points;
  }

  void updateProgress() {
    // update
  }
}
